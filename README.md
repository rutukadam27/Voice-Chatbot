# Voice-Chatbot
Voice-Based Chatbot is a Python application that interacts with users using voice commands. It listens to user input through a microphone, processes the query, and responds using text-to-speech.
